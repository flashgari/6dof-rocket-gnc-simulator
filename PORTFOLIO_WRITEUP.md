# 6-DOF Rocket Flight Simulator with TVC, LQR, and Monte Carlo Verification

## Project Objective

This project is a Python-based launch-vehicle GNC simulation and verification workflow. The goal is to model nonlinear rigid-body ascent dynamics, introduce realistic disturbance mechanisms, demonstrate open-loop failure, stabilize the vehicle with attitude feedback, implement actuator-realistic thrust vector control, compare PD and LQR control, and evaluate robustness with a Monte Carlo dispersion campaign.

The project is structured around the same logic used in flight-dynamics and controls verification:

```text
derive dynamics -> build nominal simulation -> add disturbances -> observe failure -> design controller -> model actuator -> verify robustness
```

## System Model

The vehicle state is:

```text
x = [r_I, v_I, q_BI, omega_B]
```

where `r_I` and `v_I` are inertial position and velocity, `q_BI` maps body-frame vectors into the inertial frame, and `omega_B` is body angular velocity.

The translational dynamics are:

```text
m r_ddot_I = R_BI(q) F_B + [0, 0, -mg]
```

The rotational dynamics are:

```text
I omega_dot_B + omega_B x (I omega_B) = tau_B
```

Quaternions are used instead of Euler angles because the disturbed open-loop cases pass through inverted flight and full tumble. The quaternion norm is checked because non-unit quaternions no longer represent valid rotations, which would corrupt thrust and aerodynamic force projection.

## Disturbance And Aerodynamic Model

The disturbed case includes crosswind, thrust misalignment, thrust offset, drag, angle-of-attack normal force, and CP/CM moment arm.

Relative wind is computed in body coordinates:

```text
v_rel,B = R_IB(q)(v_I - wind_I)
```

The simplified angle-of-attack normal-force model uses:

```text
qbar = 0.5 rho |v_rel|^2
F_N ~ qbar S C_N_alpha alpha
tau_aero = (r_CP - r_CM) x F_N
```

The demonstration vehicle intentionally uses a destabilizing CP/CM configuration. That creates a clear open-loop failure case: disturbance moments produce angular acceleration, angular acceleration builds angular rate, angular rate becomes attitude error, and attitude error rotates the thrust vector away from inertial up.

## Open-Loop Failure

The open-loop vehicle reaches a maximum tilt of `177.63 deg` and final altitude of only `3.99 m` after the disturbed `3 s` ascent window.

The physical mechanism is thrust-vector projection:

```text
T_vertical = T cos(theta)
T_lateral = T sin(theta)
```

As `theta` grows, vertical thrust authority collapses and horizontal acceleration grows. This is why the altitude peaks and lateral drift reaches `25.10 m`. The vehicle is not simply being pushed sideways by wind; it is converting engine impulse into crossrange motion because it has lost attitude control.

## Ideal-Torque Attitude Control

Week 3A validates the feedback law before actuator allocation. The controller regulates the thrust-axis vector:

```text
z_body,I = R_BI(q)[0, 0, 1]
e_I = z_body,I x z_cmd,I
tau_cmd,B = Kp e_B - Kd omega_B
```

The proportional term provides rotational stiffness, while the derivative term damps angular kinetic energy in the transverse modes. With ideal torque, the controller limits maximum tilt to `9.88 deg` and lateral drift to `5.65 m`.

This case is not meant to be actuator-realistic. It verifies that the attitude feedback law stabilizes the nonlinear rigid body when commanded moment is available.

## Thrust Vector Control

Week 3B replaces ideal torque with a finite TVC actuator. Moment is generated by tilting thrust through an engine lever arm:

```text
tau_TVC = r_engine x F_thrust
```

For an engine located below the center of mass:

```text
tau_x = L F_y
tau_y = -L F_x
tau_max,TVC ~= L T sin(delta_max)
```

This is the key actuator-realistic coupling: lateral thrust generates corrective moment, but the same lateral thrust also contributes to translational lateral acceleration.

The PD TVC result limits maximum tilt to `12.94 deg`, reaches final altitude `30.97 m`, and keeps gimbal saturation at `0.0%`. Compared with ideal torque, it has more lateral drift because moment generation is no longer decoupled from force direction.

## LQR Controller

Week 4A adds a local optimal-control design around upright ascent. Each transverse attitude axis is approximated as:

```text
theta_dot = omega
omega_dot = tau / I
```

The LQR controller minimizes:

```text
J = integral(x^T Q x + u^T R u) dt
```

`Q` penalizes attitude error and angular rate; `R` penalizes control effort. Because the LQR torque request is allocated through TVC, control effort also relates to gimbal demand and lateral thrust usage.

The LQR TVC result reaches final altitude `31.43 m`, limits maximum tilt to `10.30 deg`, reduces lateral drift to `10.73 m`, and keeps gimbal saturation at `0.0%`. Relative to PD TVC, the selected LQR weights reduce the transverse attitude excursion and integrated lateral drift while staying inside the same actuator envelope.

The important limitation is that LQR is local. It is designed around the upright operating point and is not a global tumble-recovery proof. The project therefore verifies LQR in the nonlinear plant rather than only reporting linear-model behavior.

## Monte Carlo Robustness Campaign

Week 4B evaluates robustness over 100 randomized dispersions per controller. The dispersions include:

- wind speed and direction
- mass
- inertia scale
- thrust magnitude
- thrust misalignment
- thrust offset
- center-of-pressure location
- normal-force slope
- maximum gimbal angle

Pass/fail criteria:

```text
max tilt < 25 deg
final altitude > 20 m
max lateral drift < 25 m
gimbal saturation fraction < 10%
```

Results:

| Controller | Success rate | Median max tilt | Median max lateral drift | Worst max tilt | Worst lateral drift |
| --- | ---: | ---: | ---: | ---: | ---: |
| Open loop | 1.0% | 177.04 deg | 24.29 m | 179.82 deg | 27.74 m |
| PD TVC | 100.0% | 12.05 deg | 12.69 m | 22.88 deg | 23.56 m |
| LQR TVC | 100.0% | 9.67 deg | 10.35 m | 17.92 deg | 19.10 m |

The open-loop success rate shows that the uncontrolled vehicle has almost no robustness margin over the selected uncertainty set. The closed-loop results show that both TVC controllers keep the vehicle inside the defined ascent envelope. LQR TVC reduces median and worst-case tilt/drift relative to PD TVC in this campaign, which indicates improved margin for the selected `Q/R` weights and dispersions.

## What The Plots Prove

The Week 2 plots prove the failure mechanism: disturbance moments rotate the thrust axis, and the trajectory fails because thrust projection changes.

The Week 3A plots prove the feedback law can stabilize the nonlinear attitude dynamics with bounded ideal torque.

The Week 3B plots prove the same stabilization objective remains feasible with a finite TVC actuator, while exposing the attitude-translation coupling introduced by gimbaled thrust.

The Week 4A plots prove that a local LQR design can be inserted into the nonlinear plant and improve attitude/lateral-drift performance relative to PD TVC without saturating the actuator.

The Week 4B plot proves robustness statistically over a defined dispersion set, which is stronger evidence than a single nominal run.

## Limitations

- Aerodynamics use a simplified normal-force model, not full coefficient tables.
- Mass, inertia, and thrust are constant during the simulated burn.
- TVC is instantaneous; actuator rate limits and servo dynamics are not modeled.
- LQR is local to upright ascent and is not a global recovery controller.
- Monte Carlo dispersions are representative for this project, not mission-certified requirements.

## Next Improvements

- Add actuator rate limits and gimbal dynamics.
- Add sensor models and an estimator.
- Add mass depletion and time-varying inertia.
- Add atmosphere variation with altitude.
- Add gain scheduling across dynamic pressure and mass states.
- Replace the simplified aero model with coefficient tables or CFD-derived lookup data.

## Interview Talking Points

- Quaternions avoid singularities during large attitude excursions and tumble.
- CP/CM offset creates aerodynamic moment through `(r_CP - r_CM) x F_N`.
- Open-loop failure is caused by disturbance moments feeding attitude error, which changes thrust projection.
- TVC is actuator-realistic because moment authority is limited by `L T sin(delta_max)`.
- TVC couples attitude correction to lateral acceleration because lateral thrust is the moment-generation mechanism.
- LQR is designed on a local linear model and must be verified on the nonlinear plant.
- Monte Carlo analysis demonstrates robustness over uncertainty, not just nominal performance.
